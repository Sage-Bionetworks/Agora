export const environment = {
  production: NODE_ENV === 'production' ? true : false,
  app_env: APP_ENV, // develop, staging, prod
  api_host: API_HOST,
  api_port: API_PORT,
  version: VERSION,
  data_version: DATA_VERSION,
  ga: 'G-Y07DD946E4',
};
